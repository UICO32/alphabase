# 项目进度

更新时间：2026-08-10

## 已完成

- 画布进入视角改为内容自适应：首次进入无有效视角的画板执行 fitView。
- 按画板保存并恢复用户上次使用的视角；切换画板时保存前一画板视角。
- 兼容旧版固定视角 { x: 0, y: 0, zoom: 1 }，将其视为“尚未使用”。
- 已提交并推送：25819a7 feat(canvas): persist and restore board viewport。
- 视角相关回归测试通过；提交钩子中的 TypeScript 检查通过。

## 进行中

### 主题研究 / 项目画布

工作区中存在一组尚未提交的项目功能改动，当前包含：

- 顶部 TopicBar：创建、切换、重命名和删除问题。
- 将卡片或 Frame 标记为成果，并从主题栏定位回画布节点。
- 项目元数据独立保存到 projects/<boardId>.json。
- 项目 store、工作区加载、同步写入/删除及拖拽置入。
- 对应的 store、成果菜单和主题栏 E2E 测试。

这组改动与视角持久化相互独立，后续应单独审查、验证和提交。

## 当前整理结果

- 保留项目功能代码及有明确断言的回归测试。
- 清理明显的 debug、重复临时测试和本地采集/实验产物。
- dist/、dist-electron/ 和现有 worktree 暂保留，避免影响本地运行或其他工作。

## 待处理

- 完成主题研究 / 项目画布的交互验证和代码审查。
- 确认项目功能范围后再单独提交，不与画布视角提交混合。
